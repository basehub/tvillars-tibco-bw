CREATE TABLE BIZDATA_LOG (
  TRANSACTION_ID CHAR(50 CHAR), 
  SOURCE_OR_TARGET CHAR(1 CHAR) DEFAULT 'S' NOT NULL ENABLE, 
  SYSTEM VARCHAR2(100 BYTE), DESCRIPTION VARCHAR2(4000 BYTE),
  DATA_AS_XML CLOB, 
  DATA_AS_BINARY BLOB, 
  TRANSACTION_DATE TIMESTAMP (6) NOT NULL ENABLE
)